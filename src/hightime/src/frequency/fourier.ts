
export class DFT {
    bufferSize: any;
    sampleRate: any;
    spectrum = []
    constructor (bufferSize: any, sampleRate: any) {
        this.bufferSize = bufferSize
        this.sampleRate = sampleRate
    }

    forward = () => {
        
    }
}

export class FFT {
    bufferSize: any;
    sampleRate: any;
    spectrum = []
    constructor (bufferSize: any, sampleRate: any) {
        this.bufferSize = bufferSize
        this.sampleRate = sampleRate
    }

    forward = () => {

    }
}